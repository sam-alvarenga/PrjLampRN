import { StyleSheet } from "react-native"

export const MensageStyles = StyleSheet.create({
  textMessage: {
    backgroundColor: '#000',
    color: '#fff',
    textAlign: 'center',
    paddingBottom: 10,
    width: '100%',
  },

})